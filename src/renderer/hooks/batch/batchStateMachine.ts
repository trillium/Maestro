// Promoted to src/shared/batch/batchStateMachine.ts as part of the
// AutoRun cross-fork drawdown (ISC-44). This file is a back-compat
// re-export so existing renderer-side imports (and tests that reference the
// renderer path directly) keep resolving.
export * from '../../../shared/batch/batchStateMachine';
