/**
 * Re-export stripAnsiCodes from shared/stringUtils for backwards compatibility.
 * The canonical implementation lives in src/shared/stringUtils.ts.
 */
export { stripAnsiCodes as stripAnsi } from '../../shared/stringUtils';
